apiVersion: v1
kind: Service
metadata:
  name: {{ template "dcs-social.fullname" . }}
  labels:
    app: {{ template "dcs-social.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - name: dcs-social-svc
    port: {{ .Values.servicePort | default "8816" }}
    targetPort: {{ .Values.servicePort | default "8816" }}
  clusterIP: None
  selector:
    app: {{ template "dcs-social.fullname" . }}
