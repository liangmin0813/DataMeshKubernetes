apiVersion: v1
kind: Service
metadata:
  name: {{ template "dcs-director.fullname" . }}
  labels:
    app: {{ template "dcs-director.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - name: dcs-director-svc
    port: {{ .Values.servicePort | default "10910" }}
    targetPort: {{ .Values.servicePort | default "10910" }}
  clusterIP: None
  selector:
    app: {{ template "dcs-director.fullname" . }}
