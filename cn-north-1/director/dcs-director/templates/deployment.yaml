apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "dcs-director.fullname" . }}
  labels:
    app: {{ template "dcs-director.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.replicas | default "3" }}
  template:
    metadata:
      labels:
        app: {{ template "dcs-director.fullname" . }}
    spec:
      containers:
      - name: {{ template "dcs-director.fullname" . }}
        image: "{{ .Values.image | default "registry.ap-southeast-1.aliyuncs.com/datamesh/dcs-director-director" }}:{{ .Values.imageTag }}"
        imagePullPolicy: {{ .Values.imagePullPolicy | quote }}
        resources:
{{ toYaml .Values.resources | indent 10 }}
        ports:
          - name: dcs-direct-svc
            containerPort: {{ .Values.servicePort | default "10910" }}
        livenessProbe:
          httpGet:
            path: /healthz
            port: {{ .Values.servicePort | default "10910" }}
          initialDelaySeconds: 15
          periodSeconds: 15
          timeoutSeconds: 5
        volumeMounts:
          - name: datadir
            mountPath: /data/director/data
          - name: service-conf
            mountPath: /data/director/conf
      imagePullSecrets:
        - name: aliyun-secret
      volumes:
        - name: service-conf
          configMap:
            name: {{ template "dcs-director.fullname" . }}
{{- if .Values.persistentVolume.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: datadir
        annotations:
        {{- range $key, $value := .Values.persistentVolume.annotations }}
          {{ $key }}: {{ $value }}
        {{- end }}
      spec:
        accessModes:
        {{- range .Values.persistentVolume.accessModes }}
          - {{ . | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.persistentVolume.size | quote }}
      {{- if .Values.persistentVolume.storageClass }}
      {{- if (eq "-" .Values.persistentVolume.storageClass) }}
        storageClassName: ""
      {{- else }}
        storageClassName: "{{ .Values.persistentVolume.storageClass }}"
      {{- end }}
      {{- end }}
{{- else }}
        - name: datadir
          emptyDir: {}
{{- end }}
