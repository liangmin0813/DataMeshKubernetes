apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "dcs-library-fe.fullname" . }}
  labels:
    app: {{ template "dcs-library-fe.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
  library.conf: |-
    server {
        listen {{ .Values.servicePort | default "80" }};
        server_name {{ .Values.libraryServerName }};
    
      location / {
          root /data/webpages;
          index index.html;
          try_files $uri $uri/ /index.html;
      }
    }