apiVersion: v1
kind: Service
metadata:
  name: {{ template "dcs-sso.fullname" . }}
  labels:
    app: {{ template "dcs-sso.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - name: dcs-sso-svc
    port: {{ .Values.servicePort | default "8811" }}
    targetPort: {{ .Values.servicePort | default "8811" }}
  clusterIP: None
  selector:
    app: {{ template "dcs-sso.fullname" . }}
