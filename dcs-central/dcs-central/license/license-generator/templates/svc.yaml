apiVersion: v1
kind: Service
metadata:
  name: {{ template "dcs-lic-gen.fullname" . }}
  labels:
    app: {{ template "dcs-lic-gen.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - name: dcs-lic-gen-svc
    port: {{ .Values.servicePort | default "6500" }}
    targetPort: {{ .Values.servicePort | default "6500" }}
  clusterIP: None
  selector:
    app: {{ template "dcs-lic-gen.fullname" . }}
