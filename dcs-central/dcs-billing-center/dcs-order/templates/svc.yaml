apiVersion: v1
kind: Service
metadata:
  name: {{ template "dcs-order.fullname" . }}
  labels:
    app: {{ template "dcs-order.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - name: dcs-order-svc
    port: {{ .Values.servicePort | default "9001" }}
    targetPort: {{ .Values.servicePort | default "9001" }}
  clusterIP: None
  selector:
    app: {{ template "dcs-order.fullname" . }}
