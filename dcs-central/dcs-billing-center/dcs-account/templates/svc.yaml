apiVersion: v1
kind: Service
metadata:
  name: {{ template "dcs-account.fullname" . }}
  labels:
    app: {{ template "dcs-account.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - name: dcs-account-svc
    port: {{ .Values.servicePort | default "9002" }}
    targetPort: {{ .Values.servicePort | default "9002" }}
  clusterIP: None
  selector:
    app: {{ template "dcs-account.fullname" . }}
