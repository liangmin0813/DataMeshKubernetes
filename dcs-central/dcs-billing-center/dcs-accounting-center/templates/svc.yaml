apiVersion: v1
kind: Service
metadata:
  name: {{ template "dcs-accounting-center.fullname" . }}
  labels:
    app: {{ template "dcs-accounting-center.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  ports:
  - name: dcs-accounting-center-svc
    port: {{ .Values.servicePort | default "9003" }}
    targetPort: {{ .Values.servicePort | default "9003" }}
  clusterIP: None
  selector:
    app: {{ template "dcs-accounting-center.fullname" . }}
